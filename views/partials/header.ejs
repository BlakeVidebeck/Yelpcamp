<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>YelpCamp</title>
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
		<link rel="stylesheet" href="/styles/main.css" />
	</head>
	<body>
		<nav class="navbar navbar-expand-md navbar-dark bg-dark mb-3 py-2 px-4">
			<a href="/" class="navbar-brand">YelpCamp</a>
			<button
				class="navbar-toggler"
				type="button"
				data-toggle="collapse"
				data-target="#navbarSupportedContent"
				aria-controls="navbarSupportedContent"
				aria-expanded="false"
				aria-label="Toggle navigation"
			>
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav mr-auto">
					<li class="nav-item p-1">
						<a class="nav-link" href="/campgrounds">Home</a>
					</li>
					<% if(currentUser){ %>
					<li class="nav-item p-1">
						<a class="nav-link" href="/users/<%= currentUser._id %>">Profile</a>
					</li>
					<% } %>

					<li class="p-2 d-md-none d-lg-inline-block">
						<form action="/campgrounds" method="GET" class="form-inline">
							<div class="form-group">
								<input
									type="text"
									name="search"
									placeholder="Campground search..."
									class="form-control form-control-sm"
								/>
								<input type="submit" value="search" class="btn btn-sm btn-primary ml-1" />
							</div>
						</form>
					</li>
				</ul>
				<!-- right navbar -->
				<ul class="navbar-nav ml-auto">
					<% if(!currentUser){ %>
					<li class="nav-item p-1">
						<a class="nav-link" href="/login">Login</a>
					</li>
					<li class="nav-item p-1">
						<a class="nav-link" href="/register">Sign Up</a>
					</li>
					<% } else { %>
					<li class="nav-item p-1">
						<a class="nav-link" href="#">Signed in as <%= currentUser.username %></a>
					</li>
					<li class="nav-item p-1">
						<a class="nav-link" href="/logout">Logout</a>
					</li>
					<% } %>
				</ul>
			</div>
		</nav>

		<div class="container">
			<% if(error && error.length > 0){ %>
			<div class="alert alert-danger" role="alert"><%= error %></div>
			<% } %> <% if(success && success.length > 0){ %>
			<div class="alert alert-success" role="alert"><%= success %></div>
			<% } %>
		</div>
	</body>
</html>
